describe("api call", function() {
  // These tests don't presently work, I believe I was encountering a scope issue that I still need to find a way around.
  // var searchResults;
  // var firstResult;

  // beforeEach(function() {
  //   apiCall("Words").done(function(response){
  //     searchResults = response;
  //   });
  // });

  // it("has a title", function(){
  //   console.log(searchResults);
  //   expect(searchResults).toEqual("Test, expecting fail");
  // });

});
